import 'package:flutter/material.dart';
import 'package:sanjeevani/features/home/broadcast/broadcast_request_screen.dart';

/// Add tab — shows the patient broadcast request screen.
class AddScreen extends StatelessWidget {
  const AddScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const BroadcastRequestScreen();
  }
}
